{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "ClinRand main-window capability, locked down per AGENTS.md and plan section 11.2: only dialog and fs (with no static scope) are granted. See apps/desktop/README.md for the full posture and the list of intentionally excluded capabilities.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default",
    "fs:allow-read-file",
    "fs:allow-read-text-file",
    "fs:allow-write-file",
    "fs:allow-write-text-file",
    "fs:allow-read-dir",
    "fs:allow-mkdir",
    "fs:allow-exists"
  ]
}
